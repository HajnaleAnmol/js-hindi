    var standloneValue = null;
    console.log(typeof(standloneValue))
    /* 
    1.null is aRepresentation of empty value. 
    2.typeof null is OBJECT.
    3.it's a standloneValue as well as its a DataType.
    */
    
    let notDefined;
    // typeof notDefined is undefined.
    console.log(notDefined);

    // symbol -: it is also a datatype which is unique.